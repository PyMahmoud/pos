<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Nav rail item: icon + label, rounded highlight when selected.
        Applied as the ItemContainerStyle on the sidebar ListBox in
        MainWindow.xaml. Uses only theme brush keys, so it repaints
        correctly under both Colors.Light.xaml and Colors.Dark.xaml.
    -->
    <Style x:Key="NavListBoxItemStyle" TargetType="ListBoxItem">
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="Margin" Value="0,2" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Focusable" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="Root"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}"
                            Background="Transparent">
                        <StackPanel Orientation="Horizontal">
                            <Path x:Name="IconPath"
                                  Data="{Binding IconData}"
                                  Fill="{StaticResource OnSurfaceVariantBrush}"
                                  Width="20" Height="20"
                                  Stretch="Uniform"
                                  VerticalAlignment="Center" />
                            <TextBlock x:Name="LabelText"
                                       Text="{Binding Label}"
                                       Style="{StaticResource BodyLarge}"
                                       Foreground="{StaticResource OnSurfaceVariantBrush}"
                                       Margin="12,0,0,0"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource SurfaceContainerHighBrush}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Root" Property="Background" Value="{StaticResource PrimaryContainerBrush}" />
                            <Setter TargetName="IconPath" Property="Fill" Value="{StaticResource OnPrimaryContainerBrush}" />
                            <Setter TargetName="LabelText" Property="Foreground" Value="{StaticResource OnPrimaryContainerBrush}" />
                            <Setter TargetName="LabelText" Property="FontWeight" Value="SemiBold" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
