<UserControl x:Class="PosSystem.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="{DynamicResource AppFlowDirection}">

    <Grid Background="{DynamicResource BackgroundBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="480">
            <TextBlock Text="{DynamicResource SettingsHeading}"
                       Style="{StaticResource DisplayLarge}"
                       Foreground="{DynamicResource OnSurfaceBrush}"
                       HorizontalAlignment="Center" />
            <TextBlock Text="{DynamicResource SettingsDescription}"
                       Style="{StaticResource BodyLarge}"
                       Foreground="{DynamicResource OnSurfaceVariantBrush}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,8,0,24" />

            <!-- Language toggle. Reuses ThemeToggleButtonStyle (NavStyles.xaml)
                 for a consistent look with the theme toggle in the sidebar
                 footer — that button itself is untouched, still lives there. -->
            <Button x:Name="LanguageToggleButton"
                    Content="{DynamicResource LanguageToggleLabel}"
                    Click="LanguageToggleButton_Click"
                    Style="{StaticResource ThemeToggleButtonStyle}"
                    Padding="16,12"
                    HorizontalAlignment="Center"
                    HorizontalContentAlignment="Center" />
        </StackPanel>
    </Grid>
</UserControl>
