<UserControl x:Class="PosSystem.App.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{DynamicResource BackgroundBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="480">
            <TextBlock Text="Customers"
                       Style="{StaticResource DisplayLarge}"
                       Foreground="{DynamicResource OnSurfaceBrush}"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Debt/credit tracking for the skincare client — 8 seeded test customers are already in rovaShop.db, ready to build this against."
                       Style="{StaticResource BodyLarge}"
                       Foreground="{DynamicResource OnSurfaceVariantBrush}"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       Margin="0,8,0,0" />
        </StackPanel>
    </Grid>
</UserControl>